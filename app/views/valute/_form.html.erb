<%= simple_form_for(@valuta) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :sigla %>
    <%= f.input :nome %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
