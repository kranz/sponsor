<%= simple_form_for(@tipo_contributo) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :descrizione %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Salva", :class => "btn btn-primary" %>
  </div>
<% end %>
